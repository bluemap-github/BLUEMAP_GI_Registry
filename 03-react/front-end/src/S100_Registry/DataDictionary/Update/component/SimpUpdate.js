import React, { useState } from 'react';

const SimpUpdate = ({ TagItemType, data, onClose }) => {
    return (
        <div>
            {JSON.stringify(data)}
        </div>
    );
}
export default SimpUpdate;