import React, { useState } from 'react';

const EnumUpdate = ({ TagItemType, data, onClose }) => {
    return (
        <div>
            {JSON.stringify(data)}
        </div>
    );
}
export default EnumUpdate;