import React, { useState } from 'react';

const CompUpdate = ({ TagItemType, data, onClose }) => {
    return (
        <div>
            {JSON.stringify(data)}
        </div>
    );
}
export default CompUpdate;