import React, { useState } from 'react';

const FeatUpdate = ({ TagItemType, data, onClose }) => {
    return (
        <div>
            {JSON.stringify(data)}
        </div>
    );
}
export default FeatUpdate;